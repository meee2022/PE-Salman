# نظام التصميم — "العنابي المؤسسي"

هوية بصرية غنية مستوحاة من الطابع المؤسسي القطري: العنابي الوطني + الذهبي + نمط هندسي إسلامي خفيف، مع ظلال ناعمة وحركات دخول هادئة.

## الألوان

| الاسم | القيمة | الاستخدام |
|------|--------|-----------|
| primary | `#5C1523` | اللون الأساسي (عنابي) |
| primary-dark | `#4A0F1B` | تدرّجات داكنة |
| gold | `#C9A96E` | اللمسات الذهبية، التمييز |
| cream | `#FCF9F2` | الخلفية |
| ink | `#2A1418` | النصوص الأساسية |

## المكوّنات

- **`PageHeader`** — بانر عنابي متدرّج بنمط زخرفي ووهج ذهبي + أيقونة + زر إجراء (يفتتح كل صفحة).
- **`KpiCard`** — بطاقة مؤشر بشريط لوني علوي ودائرة أيقونة متدرّجة + حركة دخول.
- **`StatusBadge`** — شارة حالة بنقطة لونية.
- **`ComingSoon`** — صفحة مبدئية موحّدة.
- **`Sidebar`** — شريط جانبي عنابي متدرّج بنمط زخرفي وشعار ذهبي ومؤشر نشط ذهبي.

## أصناف CSS المساعدة

- `.card` / `.card-hover` — البطاقة مع ظل ناعم ورفع عند المرور.
- `.data-table` — جدول أنيق (ترويسة متدرّجة، زِبرا، تمييز عند المرور).
- `.icon-orb` — دائرة أيقونة متدرّجة 48×48.
- `.pill` + `.badge-*` — شارات.
- `.btn-primary` (متدرّج) / `.btn-ghost` — أزرار.
- `.field` — حقل إدخال.
- `.progress-track` / `.progress-fill` — شريط تقدّم ذهبي متدرّج.
- `.pattern-arabesque` — نمط هندسي إسلامي خفيف.
- `.animate-in` — حركة دخول ناعمة (مع `animationDelay` للتتابع).

## المبادئ

1. **هوية غنية في كل صفحة** — بانر `PageHeader` العنابي + بطاقات لا صناديق بيضاء.
2. **إطار موحّد** عبر `app/dashboard/layout.tsx` (Sidebar) لكل صفحات `/dashboard/*`.
3. **تدرّج بصري** بأوزان Cairo من 300 إلى 900، ولمسات ذهبية.
4. **حركة هادئة** — دخول العناصر بتأخير متدرّج.
5. **RTL كامل** والخط Cairo.

> ملاحظة: المستخدم يفضّل هذا الثراء البصري — لا تبسّطه. عند طلب التحسين، زِد الصقل لا الإزالة.
